<h1>Burger!</h1>

<h2>Eat Dees Burgs Bruh</h2>

<ul>
  {{#each burgers}}
    {{#unless devaured}}
      {{> burgers/burger-block devaured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgs That Got Got!</h2>

<ul>
  {{#each burgers}}
    {{#if devaured}}
      {{> burgers/burger-block devaured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Type of Burger:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devaured">Devaured?</label><br>
    <input type="radio" name="devaured" value="1" checked> Devaured!<br>
    <input type="radio" name="devaured" value="0"> Still Good
  </div>

  <button type="submit">Add Burger</button>
</form>